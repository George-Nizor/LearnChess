engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false
# Use hoisted node_modules: pnpm's default symlinked store breaks on the
# Windows→WSL UNC path (\\wsl.localhost\...). Hoisted is npm-compatible.
node-linker=hoisted
